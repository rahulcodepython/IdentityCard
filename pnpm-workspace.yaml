packages:
  - "apps/*"

allowBuilds:
  sharp: true
  unrs-resolver: true
  msw: false
  # Optional better-auth adapter deps pulled in transitively; unused since
  # this app uses the Postgres/Pool adapter (see apps/web/lib/auth.ts).
  '@prisma/client': false
  better-sqlite3: false
