-- Not restoring the old plans shape or the addons tables — see CLAUDE.md,
-- this is a prototype and the old model is gone for good, not versioned
-- alongside the new one.
DROP TABLE IF EXISTS plans;
